export type DaumAddress = {
    address: string;
    autoJibunAddress: string;
    roadAddress: string;
    sigunguCode: string;
};

export type Address = {
    zip: string;
    address1: string;
    address2: string | '';
};
